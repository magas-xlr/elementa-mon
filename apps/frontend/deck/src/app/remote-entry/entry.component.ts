import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  standalone: true,
  imports: [CommonModule],
  selector: 'app-deck-entry',
  template: `deck works!`,
})
export class RemoteEntryComponent {}
