import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  standalone: true,
  imports: [CommonModule],
  selector: 'app-inventory-entry',
  template: `inventory works!`,
})
export class RemoteEntryComponent {}
