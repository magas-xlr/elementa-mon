import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  standalone: true,
  imports: [CommonModule],
  selector: 'app-avatar-entry',
  template: `avatar works!`,
})
export class RemoteEntryComponent {}
