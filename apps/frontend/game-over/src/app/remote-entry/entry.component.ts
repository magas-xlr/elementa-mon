import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  standalone: true,
  imports: [CommonModule],
  selector: 'app-game-over-entry',
  template: `game over works!`,
})
export class RemoteEntryComponent {}
